[project]
name = "fastlio-map-eval"
version = "0.1.0"
description = "Local, CPU-only offline point-cloud validation environment"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=1.26.4,<3",
    "scipy>=1.12,<2",
    "matplotlib>=3.8,<4",
    "open3d-cpu==0.19.0",
]

[tool.uv]
package = false
